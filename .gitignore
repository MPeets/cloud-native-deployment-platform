node_modules
__pycache__/
*.py[cod]

.env
.aws
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log

# Windows: accidental file created by bad redirects (e.g. `> NUL` in Git Bash)
/NUL

# Local Terraform variable overrides (not tracked)
infra/terraform.tfvars
