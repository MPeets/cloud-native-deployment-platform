**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log

# Local overrides from template (often contains personal IPs). Only this path is ignored;
# tracked files under envs/*/terraform.tfvars are shared template defaults for CI/portfolio.
/terraform.tfvars