**/.terraform/
.tflint.d/
*.tfstate
*.tfstate.*
*.tfplan
crash.log

# Local overrides from template (not tracked). Tracked files under envs/*/terraform.tfvars
# are shared template defaults for CI/portfolio.
/terraform.tfvars